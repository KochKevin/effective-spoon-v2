version: "2"

sql:
  - engine: "sqlite"
    schema: "./db/migrations/"
    queries: "./db/queries/"

    gen:
      go:
        package: "sqlc"
        out: "internal/infrastructure/sqlite/generated/"
        emit_interface: true
        emit_json_tags: true
        emit_pointers_for_null_types: true